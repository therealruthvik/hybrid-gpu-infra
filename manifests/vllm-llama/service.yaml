apiVersion: v1
kind: Service
metadata:
  name: vllm-llama
  namespace: gpu-workloads
  labels:
    app: vllm-llama
spec:
  selector:
    app: vllm-llama
  ports:
  - name: http
    port: 8001
    targetPort: 8001
  type: ClusterIP
